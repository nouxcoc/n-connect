import React from 'react';
// import PropTypes from 'prop-types';

const NewProjects = () => {
    return (
        <div className="card bg-secondary text-white rounded card-spacing">
            <h6 className="font-weight-bold mb-0 opac-5">NEW PROJECTS</h6>
            <h1 className="mb-0 custom-font font-weight-bold">4</h1>
        </div>
    );
};

NewProjects.propTypes = {
    // todo: PropTypes.object.isRequired,
};

export default NewProjects;
